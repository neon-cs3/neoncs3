version = 1

cloudstream {
    description = "DiziPal Türkiye katalog ve bölüm sağlayıcısı"
    authors = listOf("DiziAsya")
    status = 1
    tvTypes = listOf("TvSeries")
    language = "tr"
    iconUrl = "https://diziasya.com/favicon.ico"
}

android {
    buildFeatures {
        buildConfig = true
    }
}
